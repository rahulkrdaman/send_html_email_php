Author: Rahul Kr Daman

============ Instructions ============
1. Open the "index.php" file on a editor ===> Specify the recipient email ($to), sender email ($from), and sender name ($fromName).

2. The $htmlContent variable holds the HTML content. Open the email_template.html file and modify as per your need.

3. Open the "index.php" file on the browser ===> An email with HTML content will be sent to the specified recipient address.
